---
- name: Check container runtime and include appropriate tasks
  include_tasks: "{{ container_runtime }}_tasks.yaml" 
# Dynamically include tasks based on the container_runtime variable and execute them.